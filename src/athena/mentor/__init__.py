"""Mentor — the documentation module (Athena's Confluence).

Holds spaces, pages (a tree), and page versions. Like aegis/, each concept gets
a data-access module (the SQL) and the REST surface lives in api.py. The web
layer is a thin client over that API.
"""
